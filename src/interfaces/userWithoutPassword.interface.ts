interface UserWithoutPasswordInterface {
    id: string;
    username: string;
    first_name: string;
    last_name: string;
    email: string,
    role: string,
    age: string
}

export default UserWithoutPasswordInterface;